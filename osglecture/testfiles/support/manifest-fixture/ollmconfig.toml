schema = 2
bundle_preset = "OSG lecture/1"

[project]
id = "manifest-fixture"
tex_directory = "Include"
tex_config = "projectconfig.tex"

[targets.defaults]
languages = ["de", "en"]
default_language = "de"

[targets.slides]
[targets.script]
