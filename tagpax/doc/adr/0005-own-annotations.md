# ADR 0005: Own annotation and destination import

`tagpax` will extract and reconstruct supported annotations and destinations
itself. It will not require `newpax` at runtime. Any adapted third-party code
must retain attribution and licence notices.
