# OLLM tests

The presence of this directory makes `l3build check` run the module's
`checkinit_hook`, which currently performs the Perl syntax check.

Stable CLI, manifest, discovery, and integration tests will be added here as
the new implementation is developed.
