#!/usr/bin/env perl

use v5.30;
use strict;
use warnings;

use FindBin qw($RealBin);
use lib "$RealBin/vendor/TOML-Tiny-0.22/lib";
use lib "$RealBin/lib";

use OLLM::CLI;

exit OLLM::CLI->run(
  argv       => \@ARGV,
  definitions_dir => "$RealBin/definitions",
  program    => $0,
  script_dir => $RealBin,
);
