% The selectable languages and their order come from the project manifest
% ([targets.defaults].languages); the class hands them to langselect from the
% build file, so a single langselect-aware source needs no languages setup
% here. \olsTargetLanguage is likewise bridged from the per-build language
% before langselect loads. \title is deferred through osglecture's metadata
% queue and replayed after \LoadClass, so \lende is defined by the time it
% runs.
\OsgLectureBuildLoadedTF
  { \edef\olsTargetLanguage{\OsgLectureBuildValue{language}} }
  { }
\title{\lende{One source, multiple documents}{Eine Quelle, mehrere Dokumente}}
\author<presentation>[M.~Mustermann]{Max Mustermann}
\author<longform>[Mustermann]{Max Mustermann}
\OsgLectureSetup{
  mode/longform = {
    continuation = {counters={page,section}}
  }
}
