schema = 1
bundle_preset = "OSG lecture/1"

[project]
id = "example"

[project.tex]
directory = "Include"
config = "projectconfig.tex"

[languages]
available = ["de", "en"]
default = "de"

[targets.slides]
languages = ["de"]

[targets.script]
languages = ["de", "en"]

[security]
shell_escape = "restricted"
