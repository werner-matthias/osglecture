\begin{frame}
  \frametitle{\lende{One source, two presentations}%
    {Eine Quelle, zwei Darstellungen}}
  \begin{presitemize}
    \item \lende{a unit contains the subject-matter content}%
      {eine Unit enthält den fachlichen Inhalt}
    \anditem \lende*{target @1 produces a presentation}%
      {das Target @1 erzeugt eine Präsentation}{\emph{talk}}
    \anditem \lende*{target @1 produces a long form}%
      {das Target @1 erzeugt eine Langform}{\emph{script}}
    \thereforeitem \lende{we \finite{need} to maintain changes only once}%
      {wir \finite{müssen} Änderungen nur einmal pflegen}
    \item \lende{language-independent data needs to be entered only once}%
      {sprachunabhängige Daten müssen nur einmal angegeben werden}%
      \only<article>{,
      \lende{since they can be inserted into the text using @-parameters}%
        {da sie über @-Parameter in den Text eingefügt werden können}}
  \end{presitemize}
\end{frame}
