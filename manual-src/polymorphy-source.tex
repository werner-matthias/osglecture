\begin{frame}
  \frametitle{\lende{One Source, Many Presentations}{Eine Quelle, viele Darstellungen}}
  \begin{presitemize}
    \item \lende{a unit contains the subject-matter content}%
      {eine Lehreinheit enthält den fachlichen Inhalt}
    %
    \item \lende*{target @1 produces a presentation}%
      {das Target @1 erzeugt eine Präsentation}{\emph{talk}}%
    %
    \anditem \lende*{target @1 produces a long form}%
      {das Target @1 erzeugt eine Langform}{\emph{script}}%
    %
    \thereforeitem \lende{we \finite{need} to maintain changes only once}%
      {wir \finite{müssen} Änderungen nur an einer Stelle pflegen}%
    %  
    \item \lende{language-independent data needs to be entered at one place}%
      {sprachunabhängige Daten müssen nur einmal angegeben werden}%
     % 
      \only<article>{, \lende{since they can be inserted into the text using @-parameters}%
        {da sie über @-Parameter in den Text eingefügt werden können}.%
      }%
  \end{presitemize}%
\end{frame}
\lende*{Text written outside the @1 environment is reserved exclusively for the long form.}%
{Außerhalb der @1-Umgebung geschriebener Text ist exklusiv der Langform vorbehalten.}{\texttt{frame}}