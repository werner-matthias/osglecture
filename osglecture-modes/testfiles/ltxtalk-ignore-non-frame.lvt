\input regression-test.tex
\DocumentMetadata{}
\documentclass{ltx-talk}
% Loading the generated Lua helper is itself under test; keep the scanner off
% here because the regression harness is supplied through an outer \input.
\usepackage[ignorenonframetext=false]{osglecture-modes-ltxtalk}
\begin{document}
\START
\TYPE{LTXTALK LUA HELPER LOADED}
\END
\end{document}
